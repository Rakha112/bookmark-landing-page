import React from "react";
import hero from "../images/illustration-hero.svg";
const Hero = () => {
  return (
    <div className="hero">
      <img src={hero} alt="" />
    </div>
  );
};

export default Hero;
